# Audit Log — {{ dataset_name }}

**Generated:** {{ metadata.assessment_date[:10] }}
**Chain Root:** `{{ chain_root or "N/A" }}`
**Integrity:** {{ "VERIFIED" if metadata.merkle_chain_verified else "UNVERIFIED" }}

## Provenance Records ({{ metadata.provenance_records_count }} total)

| # | Record ID | Name | Source | Rows | Timestamp | Merkle Root |
|---|-----------|------|--------|------|-----------|-------------|
{% for c in metadata.chain_of_custody %}| {{ loop.index }} | `{{ c.record_id }}` | {{ c.name }} | {{ c.source or "-" }} | {{ c.row_count or "-" }} | {{ c.timestamp[:19] }} | `{{ c.fingerprint }}` |
{% endfor %}

## Data Sources

{% for src in metadata.data_sources %}- **{{ src.uri }}**{% if src.owner %} (Owner: {{ src.owner }}){% endif %}
{% endfor %}{% if not metadata.data_sources %}- None recorded
{% endif %}

{% if metadata.trust_score is not none %}
## Trust Assessment

- **Score:** {{ metadata.trust_score }}/100 ({{ metadata.trust_grade }})
- **Assessment Date:** {{ metadata.assessment_date[:19] }}
{% endif %}

---
*Generated by [TrustPipe](https://github.com/pallavi-chandrashekar/trustpipe)*
